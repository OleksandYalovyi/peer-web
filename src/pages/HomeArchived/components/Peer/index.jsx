import React from 'react'
import styles from './styles.module.scss'

const Peer = () => (
  <section className={styles.peer}>
    <h2 className={styles.title}>
      we’re building <br /> the living metaverse
    </h2>
  </section>
)

export default Peer
